<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.BLUETOOTH_SCAN"/>
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>
  <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"/>

  <application

        android:allowBackup="true"
        android:icon="@mipmap/rss"
        android:label="Unirss"
        android:roundIcon="@mipmap/rss"
        android:supportsRtl="true"
        android:theme="@android:style/Theme">
        <activity
            android:name="com.jedugsem.unirss.MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|locale|keyboardHidden"
            android:windowSoftInputMode="stateVisible|adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data android:name="android.app.lib_name" android:value="unirss" />
        </activity>
    </application>

</manifest>
